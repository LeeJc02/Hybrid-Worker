interface:
  display_name: "Hybrid Worker"
  short_description: "Dynamic DAG hybrid mode with bounded Claude workers and transactional manager merges"
  default_prompt: "Use $hybrid-worker for explicit hybrid mode. Run v2 plan-only first; create exactly three Codex managers only when execution_mode is hierarchical, keep all Claude stages behind the shared broker, and finalize through the parent-run transaction."
